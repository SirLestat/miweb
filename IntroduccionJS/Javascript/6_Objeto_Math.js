// escribir window en la consola permite ver todas las funciones de JS

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); // redondea numeros .5 hacia arriba .4 hacia abajo
resultado = Math.ceil(2.1) ; // siempre redondea hacia arriba
resultado = Math.floor(2.9); // siempre redondea hacia abajo
resultado = Math.sqrt(144); // Raiz cuadrada
resultado = Math.abs(-200); // convierte a positivo
resultado = Math.min(1,8,9,6,3,4,8); //devuelve el mas pequeño de una serie de numeros
resultado = Math.max(1,9,8,5,2,3,54); // devuleve el mas grande de una serie de numeros
resultado = Math.random(); // genera un numero aleatorio entre 0 y 1
resultado = Math.floor(Math.random()*10); // genera un numero aleatorio entre el 0 y el 9



console.log(resultado);